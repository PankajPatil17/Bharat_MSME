import 'package:flutter/material.dart';

const mainColor = Color(0xffD82A1B);
const mainBlue = Color(0xffBF2025);
const PWhite = Color(0xffFFFFFF);
const Mainfentbackgrey = Color(0xffC6C6C6);
const Mainboxshadow = [
  BoxShadow(
      blurRadius: 2,
      spreadRadius: 2,
      color: Colors.black12,
      offset: Offset(0, 1))
];

const FormBoxShadow = [
  BoxShadow(
      blurRadius: 1,
      spreadRadius: 1,
      color: Colors.black12,
      offset: Offset(0, 1))
];
const MainBorderRadius = BorderRadius.all(Radius.circular(12));
